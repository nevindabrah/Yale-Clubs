{
  "name": "yaleclubs",
  "version": "1.0.0",
  "private": true,
  "description": "YaleClubs — a two-portal club catalog for Yale, in the spirit of CourseTable",
  "scripts": {
    "setup": "npm --prefix server install && npm --prefix client install",
    "db:start": "bash scripts/db-start.sh",
    "db:stop": "bash scripts/db-stop.sh",
    "db:reset": "npm --prefix server run db:reset",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "test": "npm --prefix server run test:e2e"
  }
}
